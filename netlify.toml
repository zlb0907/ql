# Netlify配置文件 - 情侣同步观影应用

[build]
  # 静态站点部署，不需要构建
  command = ""
  publish = "."

# SPA路由支持
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 安全头设置
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# 静态资源缓存
[[headers]]
  for = "*.js"
  [headers.values]
    Cache-Control = "public, max-age=31536000"

[[headers]]
  for = "*.css"
  [headers.values]
    Cache-Control = "public, max-age=31536000"

# API代理配置（用于后端服务）
# ⚠️ 重要：请将 your-backend-service 替换为你的实际Render服务名称
[[redirects]]
  from = "/api/*"
  to = "https://couple-sync-video-server.onrender.com/api/:splat"
  status = 200

# WebSocket代理配置
# ⚠️ 重要：请将 your-backend-service 替换为你的实际Render服务名称
[[redirects]]
  from = "/ws"
  to = "wss://couple-sync-video-server.onrender.com"
  status = 200